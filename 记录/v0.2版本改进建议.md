我检查了你让我手动验证的几项，大部分正常，以下是异常或者需要改进的地方。
1. 抽取出记录后，点击“记入记录”可以正常跳转到编辑页，多数内容正常，但“吃的什么”显示“%E9%A3%9F%E5%A0%82”，不过似乎可以正常保存；且该框初始显示不全，仅显示上半，包括未输入时的提示文字，仅显示下半，点击该框就显示全了。
2. 样式上需要优化，包括：本次选饭结果/本次做饭结果，按钮和文字离的有的比较合适，有的很近，需要调整位置；除了“不算”和“考虑下”，下方再添一个按钮，“算了，不抽了~”，该次结果不会记入最近记录，也不会再抽下一次，也就是说，“记入记录”改为“好，就吃这个，记下来！”，表示记录且不抽，“不算，再抽一次！”表示不记录且抽，“考虑下...再抽一次”改为“记一下，再抽个别的吧~”，表示记录且抽，“算了，不抽了~”表示不记录且不抽，四个按钮按两行两列排布，先从左至右，再从上至下分别为前面我说的这四个的顺序。关于之前的选项的说明，把它浓缩为一个弹出式的说明，即“‘好，就吃这个，记下来！’即记录且不抽；”等等。用户点击在“本次做饭结果”/“本次选饭结果”的右侧的带有感叹号图标的“结果说明”按钮即可弹出规则说明。
3. 最近抽取记录中的每条记录的右侧有个“选项”，这个有点奇怪。如果该条记录未记入，请写“未记录”，如果已记录，请写“已记录”。另外，这个圆形/椭圆形的框和字体似乎没对应好。以及最近抽取记录中的每条记录中的“记入记录”和“删除”按钮有点近。
4. 下方三个按钮中的“记录”这个页面的记录中的名称如果使用“%E9%A3%9F%E5%A0%82”保存，这里的饭的名称会显示“%E9%A3%9F%E5%A0%82”，而不是正常字符。另外，每条记录应该显示这是“选饭”还是“做饭”，在下方所有记录的框右上角的按钮也应该包括“去选饭”和“去做饭”，而不是只有“去选饭”。